<local:MainViewBase x:Class="VolatileHordes.GUI.Views.MainView"
                    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                    xmlns:local="clr-namespace:VolatileHordes.GUI.Views"
                    mc:Ignorable="d"
                    d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <local:ConnectionView Grid.Row="0" x:Name="ConnectionView" Margin="4" />
        <StackPanel Orientation="Horizontal" Grid.Row="1">
            <CheckBox Content="Group Targets" x:Name="GroupTargetsBox" VerticalAlignment="Center" Margin="4" />
            <CheckBox Content="Targets" x:Name="TargetsBox" VerticalAlignment="Center" Margin="4" />
        </StackPanel>
        <local:PlayerView Grid.Row="2" x:Name="PlayerView" />
    </Grid>
</local:MainViewBase>
